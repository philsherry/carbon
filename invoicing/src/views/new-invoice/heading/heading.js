import React from 'react';
import Heading from 'carbon/lib/components/heading';

export default (props) => {
  return (
    <Heading
      title="New Invoice: Invoice 1 (Inv1)"
      separator={ true }
      divider={ false }
    />
  )
};
